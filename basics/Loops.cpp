#include <iostream>
#include <math.h>

using namespace std;

int main()
{

    // for loop.
    for (int i = 0; i <= 10; i++)
    {
        cout << i << endl;
    }

    // while loop
    int j = 0;
    while (j < 10)
    {
        cout << pow(j, 3) << endl;
        j++;
    }

    return 0;
}